import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'marketplace',
  webDir: '../../dist/apps/marketplace',
  bundledWebRuntime: false,
};

export default config;
